@model MakeCurriculum.Models.ViewModels.FormViewModel

<div class="col-md-10 col-md-offset-1">
    <form asp-action="Create" asp-controller="Resumes" method="post">
        <div class="form-group">
            <label asp-for="Resume.Description" class="control-label"></label>
            <textarea asp-for="Resume.Description" class="form-control"></textarea>
            <span asp-validation-for="Resume.Description" class="text-danger"></span>
        </div>
        <div class="form-group text-right">
            <input asp-for="Resume.CurriculumId" value="@Model.Curriculum.Id" type="hidden" />
            <button type="submit" class="btn btn-success btn-sm"><i class="glyphicon glyphicon-ok"></i> Salvar</button>
        </div>
    </form>
</div>